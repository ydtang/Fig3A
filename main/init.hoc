

load_file("xor.hoc")

objref vbox
objref null

vbox = new VBox()
vbox.intercept(1)

xpanel("XOR")

  xbutton("uncoupled/Figure 3: test ","test_pulse_uncoupled()")
  xbutton("uncoupled/Figure 3: runmodel","uncoupled(\"X\")")
  
  

xpanel()
vbox.intercept(0)	//ends intercept mode
vbox.map()		//draw the box and its contents
